{
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "moduleResolution": "Node16",
    // `type` in the package.json is `module`, so tsc emits `ES2020`
    "module": "Node16",
    "outDir": "./dist/esm"
  },
  "exclude": ["**/*.test.ts", "./src/test/"]
}
